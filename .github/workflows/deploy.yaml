name: Manual Deploy

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest


    steps:
      - uses: actions/checkout@v4

      - name: deploy
        run: echo "Deploying the application"
